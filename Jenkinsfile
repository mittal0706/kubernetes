pipeline {
  agent any
  
  stages{
    stage('git checkout'){
      steps{
        git 'https://github.com/mittal0706/kubernetes.git'
      }
    }
    stage('git checkout'){
      steps{
        git 'https://github.com/mittal0706/kubernetes.git'
      }
    }
  }
}
